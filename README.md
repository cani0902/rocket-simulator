# ROCKETLAB — Vite + React + Tailwind

A Vercel-friendly rocket flight simulation dashboard.

## Local
```bash
npm install
npm run dev
```

## Production build
```bash
npm run build
```

## Vercel
Import the GitHub repository. Vercel detects Vite automatically; build command is `npm run build` and output directory is `dist`.
